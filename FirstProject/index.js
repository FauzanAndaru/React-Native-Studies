/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppTernary from './AppTernary';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppTernary);
